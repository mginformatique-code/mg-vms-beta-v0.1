apiVersion: mgvms.io/v1
kind: Plugin
metadata:
  name: marketplace-test
  displayName: "Marketplace Test"
  version: "0.1.0"
  description: "Plugin FrameAnalyzer — description à compléter."
  author: "Votre nom"
  license: "MIT"
  categories: [detection]
spec:
  runtime: python
  entrypoint: plugin.py
  className: MarketplaceTestPlugin
  interface: FrameAnalyzer
  compatibility: {mgvms_core: ">=2.30.0,<4.0.0"}
  capabilities: [camera.frame.read, event.write]
  resources: {cpu_cores: 0.5, ram_mb: 128, gpu: none, disk_mb: 20}
  config_schema: config/schema.json
  bus:
    order: 100
