# Moteur IA — YOLOv11 + ByteTrack (PRODUCTION, GPU)
ultralytics==8.3.0          # YOLOv11 (détection + tracking intégré ByteTrack)
supervision==0.23.0         # utilitaires détection/annotation + ByteTrack
opencv-python-headless==4.10.0.84
numpy==1.26.4
redis==5.0.1
SQLAlchemy==2.0.25
psycopg[binary]==3.1.17
httpx==0.28.1
fast-alpr==0.1.1            # ANPR (détection + OCR de plaques) basé ONNX
